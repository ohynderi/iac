{
    "lldpIfPol": {
        "attributes": {
            "name": "{{ pol.name }}",
            "status": "{{ 'deleted' if pol.state == 'absent' else 'created,modified' }}"

            {% if pol.state == 'present' %}
            ,
            "descr": "{{ pol.description | default('') }}",
            "adminRxSt": "{{ pol.receive }}",
            "adminTxSt": "{{ pol.transmit }}"
            {% endif %}
        }
    }
}