{
  "infraAccPortP": {
    "attributes": {
      "name": "{{ leaf_intf_prof_name }}",
      "status": "{{ 'deleted' if leaf_intf_prof.state is defined and leaf_intf_prof.state == 'absent' else 'created,modified' }}"

      {% if leaf_intf_prof.state | default('present') != 'absent' %}
      ,
      "descr": "{{ leaf_intf_prof.description | default(leaf_intf_prof_default_description) }}"
      {% endif %}
    }
    {% if leaf_intf_prof.state | default('present') != 'absent' %}
    ,
    "children": [
      {% for tag in leaf_intf_prof.tags | default( [] ) %}
      {
        "tagAnnotation": {
          "attributes": {
            "key": "{{ tag.name }}",
            "value": "{{ tag.value }}",
            "status": "{{ 'deleted' if tag.state is defined and tag.state == 'absent' else 'created,modified' }}"
          }
        }
      }
      {% if not loop.last %},{% endif %}
      {% endfor %}
    ]
    {% endif %}
  }
}
